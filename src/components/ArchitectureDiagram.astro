<figure class="architecture">
  <div class="architecture-users">
    <div class="arch-node arch-node--user"><span class="node-meta">HUMAN</span><strong>Researchers</strong></div>
    <div class="join" aria-hidden="true"></div>
    <div class="arch-node arch-node--user"><span class="node-meta">COMPUTATIONAL</span><strong>AI systems</strong></div>
  </div>
  <div class="down" aria-hidden="true">↓</div>
  <div class="arch-node arch-node--workflow"><span class="node-meta">EXPERIMENT LAYER</span><strong>Scientific workflows</strong></div>
  <div class="down" aria-hidden="true">↓</div>
  <div class="architecture-stack">
    <div class="arch-node arch-node--primary"><span class="node-meta">SECURE ACCESS</span><strong>ChemCloud</strong></div>
    <div class="down" aria-hidden="true">↓</div>
    <div class="arch-node arch-node--primary"><span class="node-meta">DISTRIBUTION</span><strong>BigChem</strong></div>
    <div class="down" aria-hidden="true">↓</div>
    <div class="arch-node arch-node--primary"><span class="node-meta">INTEROPERABILITY</span><strong>qc*</strong></div>
  </div>
  <div class="down" aria-hidden="true">↓</div>
  <div class="qc-branches">
    <div class="arch-node arch-node--small"><strong>qcdata</strong><span>structure</span></div>
    <div class="arch-node arch-node--small"><strong>qccodec</strong><span>translate</span></div>
    <div class="arch-node arch-node--small"><strong>qccompute</strong><span>operate</span></div>
    <div class="arch-node arch-node--small"><strong>qcconst</strong><span>reference</span></div>
  </div>
  <div class="down" aria-hidden="true">↓</div>
  <div class="architecture-foundation">
    <div class="arch-node arch-node--foundation"><span class="node-meta">SCIENTIFIC SOFTWARE</span><strong>Simulation programs</strong></div>
    <div class="arch-node arch-node--foundation"><span class="node-meta">COMPUTE</span><strong>CPU / GPU / HPC / Cloud</strong></div>
  </div>
  <figcaption><span>AFH COMPUTATIONAL ARCHITECTURE</span><span>from scientific intent to physical computation</span></figcaption>
</figure>

<style>
  .architecture { margin: 3.5rem 0 0; padding: clamp(1.25rem, 4vw, 3.5rem); background: var(--bg-secondary); border: 1px solid var(--border); border-radius: var(--radius-md); }
  .architecture-users, .architecture-foundation { display: grid; grid-template-columns: minmax(0, 1fr) minmax(0, 1fr); gap: 2rem; position: relative; }
  .join { position: absolute; width: calc(50% + 1rem); height: 15px; left: calc(25% - .5rem); bottom: -15px; border: solid var(--diagram-line); border-width: 0 1px 1px; }
  .arch-node { display: flex; align-items: center; justify-content: space-between; gap: 1rem; min-height: 72px; padding: 1rem 1.4rem; border: 1px solid var(--border-strong); border-radius: var(--radius-sm); background: var(--bg-secondary); }
  .arch-node strong { color: var(--blue-dark); font: 600 1.15rem/1.3 var(--font-sans); }
  .node-meta { color: var(--text-muted); font: 500 .73rem/1.4 var(--font-sans); letter-spacing: .04em; }
  .arch-node--workflow { width: 70%; margin: 0 auto; }
  .architecture-stack { width: 56%; margin: 0 auto; }
  .arch-node--primary { background: var(--bg-blue-soft); }
  .arch-node--primary strong { color: var(--blue-dark); font-size: 1.25rem; }
  .down { height: 36px; display: grid; place-items: center; color: var(--blue-primary); font-size: 1.15rem; }
  .qc-branches { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 1rem; }
  .arch-node--small { min-height: 88px; flex-direction: column; align-items: flex-start; justify-content: center; gap: .5rem; }
  .arch-node--small strong { font: 500 .95rem/1.4 var(--font-mono); }
  .arch-node--small span { color: var(--text-secondary); font: 400 .9rem/1.3 var(--font-sans); }
  .architecture-foundation { gap: 1rem; }
  .arch-node--foundation { flex-direction: column; align-items: flex-start; gap: .5rem; }
  figcaption { display: flex; justify-content: space-between; gap: 1rem; margin-top: 2rem; padding-top: 1rem; border-top: 1px solid var(--border); color: var(--text-muted); font: 500 .75rem/1.5 var(--font-sans); letter-spacing: .035em; }
  figcaption span:last-child { letter-spacing: 0; text-align: right; }
  @media (max-width: 700px) {
    .architecture-users, .architecture-foundation { grid-template-columns: 1fr; gap: .75rem; }
    .qc-branches { grid-template-columns: repeat(2, minmax(0, 1fr)); gap: .75rem; }
    .arch-node--workflow, .architecture-stack { width: 100%; }
    .join { display: none; }
    .arch-node { padding: 1rem; }
    figcaption { flex-direction: column; gap: .25rem; }figcaption span:last-child { text-align: left; }
  }
  @media (max-width: 380px) { .arch-node { align-items: flex-start; flex-direction: column; gap: .4rem; }.arch-node--small strong { font-size: .82rem; } }
</style>

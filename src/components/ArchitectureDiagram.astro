<figure class="architecture">
  <div class="architecture-users">
    <div class="arch-node arch-node--user"><span class="node-meta">HUMAN</span><strong>Researchers</strong></div>
    <div class="join" aria-hidden="true"></div>
    <div class="arch-node arch-node--user"><span class="node-meta">COMPUTATIONAL</span><strong>AI systems</strong></div>
  </div>
  <div class="down" aria-hidden="true">↓</div>
  <div class="arch-node arch-node--workflow"><span class="node-meta">EXPERIMENT LAYER</span><strong>Scientific workflows</strong></div>
  <div class="down" aria-hidden="true">↓</div>
  <div class="architecture-stack">
    <div class="arch-node arch-node--primary"><span class="node-meta">SECURE ACCESS</span><strong>ChemCloud</strong></div>
    <div class="down" aria-hidden="true">↓</div>
    <div class="arch-node arch-node--primary"><span class="node-meta">DISTRIBUTION</span><strong>BigChem</strong></div>
    <div class="down" aria-hidden="true">↓</div>
    <div class="arch-node arch-node--primary"><span class="node-meta">INTEROPERABILITY</span><strong>qc*</strong></div>
  </div>
  <div class="down" aria-hidden="true">↓</div>
  <div class="qc-branches">
    <div class="arch-node arch-node--small"><strong>qcdata</strong><span>structure</span></div>
    <div class="arch-node arch-node--small"><strong>qccodec</strong><span>translate</span></div>
    <div class="arch-node arch-node--small"><strong>qccompute</strong><span>operate</span></div>
    <div class="arch-node arch-node--small"><strong>qcconst</strong><span>reference</span></div>
  </div>
  <div class="down" aria-hidden="true">↓</div>
  <div class="architecture-foundation">
    <div class="arch-node arch-node--foundation"><span class="node-meta">SCIENTIFIC SOFTWARE</span><strong>Simulation programs</strong></div>
    <div class="arch-node arch-node--foundation"><span class="node-meta">COMPUTE</span><strong>CPU / GPU / HPC / Cloud</strong></div>
  </div>
  <figcaption><span>AFH COMPUTATIONAL ARCHITECTURE</span><span>from scientific intent to physical computation</span></figcaption>
</figure>

<style>
  .architecture { margin: 3.5rem 0 0; padding: clamp(1.2rem, 4vw, 3.5rem); background: var(--bg-secondary); border: 1px solid var(--border-dark); border-radius: var(--radius-lg); }
  .architecture-users, .architecture-foundation { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; position: relative; }
  .join { position: absolute; width: 1rem; height: 1rem; left: calc(50% - .5rem); bottom: -.55rem; border-bottom: 1px solid color-mix(in srgb, var(--accent) 30%, transparent); transform: rotate(45deg); }
  .arch-node { display: flex; align-items: center; justify-content: space-between; gap: 1rem; min-height: 68px; padding: 1rem 1.2rem; border: 1px solid var(--border-dark); border-radius: var(--radius-sm); background: var(--bg-secondary); }
  .arch-node strong { font: 520 1rem/1.2 var(--font-mono); letter-spacing: -.03em; }
  .node-meta { color: var(--text-muted); font: 500 .61rem/1.2 var(--font-mono); letter-spacing: .1em; }
  .arch-node--user { background: color-mix(in srgb, var(--bg-elevated) 55%, transparent); }
  .arch-node--workflow { width: 66%; margin: 0 auto; border-color: color-mix(in srgb, var(--accent-cool) 25%, transparent); }
  .architecture-stack { width: 47%; margin: 0 auto; }
  .arch-node--primary { border-color: color-mix(in srgb, var(--accent) 27%, transparent); background: color-mix(in srgb, var(--accent-dark) 10%, transparent); }
  .arch-node--primary strong { color: var(--accent); font-size: 1.18rem; }
  .down { height: 30px; display: grid; place-items: center; color: var(--text-muted); font-family: var(--font-mono); }
  .qc-branches { display: grid; grid-template-columns: repeat(4, 1fr); gap: .7rem; }
  .arch-node--small { min-height: 80px; flex-direction: column; align-items: flex-start; justify-content: center; }
  .arch-node--small span { color: var(--text-muted); font: 450 .65rem/1.2 var(--font-mono); }
  .architecture-foundation { gap: .7rem; }
  .arch-node--foundation { background: transparent; }
  figcaption { display: flex; justify-content: space-between; gap: 1rem; margin-top: 2rem; padding-top: 1rem; border-top: 1px solid var(--border-dark); color: var(--text-muted); font: 500 .63rem/1.3 var(--font-mono); letter-spacing: .09em; }
  figcaption span:last-child { letter-spacing: 0; text-align: right; }
  @media (max-width: 700px) {
    .architecture-users, .architecture-foundation, .qc-branches { grid-template-columns: 1fr; }
    .arch-node--workflow, .architecture-stack { width: 100%; }
    .join { display: none; }
    .architecture-users .arch-node + .arch-node, .architecture-foundation .arch-node + .arch-node, .qc-branches .arch-node + .arch-node { margin-top: 0; }
    .qc-branches { gap: .5rem; }
    .arch-node--small { min-height: 58px; flex-direction: row; align-items: center; justify-content: space-between; }
    figcaption { flex-direction: column; }.architecture figcaption span:last-child { text-align: left; }
  }
</style>

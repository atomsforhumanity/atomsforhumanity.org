---
import Logo from "./Logo.astro";
import { navigation, organization } from "../data/organization";
---

<footer class="site-footer">
  <div class="container footer-main">
    <div class="footer-brand">
      <a href="/" aria-label="Atoms for Humanity home"><Logo /></a>
      <p>{organization.mission}</p>
    </div>
    <div class="footer-column">
      <h2>Explore</h2>
      <ul>
        {navigation.map((item) => <li><a href={item.href}>{item.label}</a></li>)}
        <li><a href="/donate">Make a Donation</a></li>
      </ul>
    </div>
    <div class="footer-column">
      <h2>Connect</h2>
      <ul>
        <li><a href={organization.links.github}>GitHub <span aria-hidden="true">↗</span></a></li>
        <li><a href={organization.links.linkedin}>LinkedIn <span aria-hidden="true">↗</span></a></li>
      </ul>
    </div>
    <div class="footer-column footer-identity">
      <h2>{organization.legalName}</h2>
      <p>{organization.type}<br />EIN {organization.ein}</p>
      <address>
        {organization.address.street}<br />
        {organization.address.city}, {organization.address.state} {organization.address.postalCode}<br />
        {organization.address.country}
      </address>
    </div>
  </div>
  <div class="container footer-bottom">
    <p>© 2026 Atoms for Humanity</p>
    <p>Building scientific infrastructure for a sustainable molecular future.</p>
  </div>
</footer>

<style>
  .site-footer { padding: clamp(4rem, 7vw, 6rem) 0 1.5rem; }
  .footer-main { display: grid; grid-template-columns: minmax(240px, 1.6fr) .75fr .65fr 1fr; gap: clamp(2rem, 4vw, 4rem); }
  .footer-brand a { text-decoration: none; }
  .footer-brand p { max-width: 310px; margin: 1.4rem 0 0; color: var(--text-secondary); font-family: var(--font-serif); font-size: 1.35rem; line-height: 1.5; }
  .footer-column h2 { margin: .25rem 0 1.3rem; font: 600 .85rem/1.4 var(--font-sans); letter-spacing: .025em; color: var(--text-primary); }
  .footer-column ul { list-style: none; padding: 0; margin: 0; }
  .footer-column li + li { margin-top: .65rem; }
  .footer-column a { color: var(--text-secondary); text-decoration: none; font-size: 1rem; }
  .footer-column a:hover { color: var(--link-color); text-decoration: underline; }
  .footer-identity p, .footer-identity address { margin: 0; color: var(--text-secondary); font-size: .98rem; line-height: 1.65; font-style: normal; }
  .footer-identity address { margin-top: 1rem; }
  .footer-bottom { display: flex; justify-content: space-between; gap: 2rem; margin-top: clamp(3rem, 6vw, 5rem); padding-top: 1.25rem; border-top: 1px solid var(--border); color: var(--text-muted); font-size: .85rem; }
  .footer-bottom p { margin: 0; }
  @media (max-width: 1000px) { .footer-main { grid-template-columns: 1.5fr 1fr 1fr; }.footer-identity { grid-column: 1 / -1; } }
  @media (max-width: 560px) { .footer-main { grid-template-columns: 1fr 1fr; }.footer-brand, .footer-identity { grid-column: 1 / -1; }.footer-bottom { flex-direction: column; gap: .5rem; } }
</style>
